const notas = [10, 6, 8, 5.5, 10];

notas.pop(); // comando POP remove o ultimo item do array

const mediaCorreta = (notas[0] + notas[1] + notas[2], notas[3]) / notas.length;
console.log(mediaCorreta);
